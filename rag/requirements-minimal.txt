# Minimal Requirements for Claude Code Deployment
# =================================================
# These are the essential packages needed to run RAG-CLI
# as a Claude Code plugin (no API key required)

# Document Processing
langchain>=0.1.0
langchain-community>=0.0.10
pypdf>=3.0.0
python-docx>=1.0.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
markdown>=3.5.0

# Embeddings & Vector Store
sentence-transformers>=2.3.1
chromadb>=1.3.0
numpy>=1.24.0

# Retrieval (bm25s is what the code actually imports)
bm25s>=0.1.0

# Structured Logging
structlog>=24.1.0

# CLI / UI
rich>=13.0.0
click>=8.1.0
tqdm>=4.65.0

# Configuration
pyyaml>=6.0
pydantic>=2.5.0

# Monitoring
psutil>=5.9.0

# Note: anthropic package is NOT required for Claude Code mode
# It's only needed if you want to run in standalone mode

# Removed (not used by the codebase):
#   PyPDF2  -> replaced by pypdf
#   faiss-cpu -> replaced by chromadb
#   rank-bm25 -> replaced by bm25s
